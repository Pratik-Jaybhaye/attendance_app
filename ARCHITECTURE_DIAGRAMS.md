# Architecture Diagram & Implementation Overview

## System Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         ATTENDANCE APP - PHOTO & EMBEDDING ARCHITECTURE      │
│                                                                               │
│  ┌──────────────┐                                                           │
│  │   User       │                                                           │
│  │  (Teacher)   │                                                           │
│  └──────┬───────┘                                                           │
│         │ Captures/Selects Photo                                            │
│         ▼                                                                    │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                    PHOTO MANAGEMENT SERVICE                          │  │
│  │              (High-level Orchestration Layer)                        │  │
│  │                                                                       │  │
│  │  Methods:                                                            │  │
│  │  • saveCapturedPhoto()                                              │  │
│  │  • uploadPhoto()                                                    │  │
│  │  • requestEmbeddingProcessing()                                     │  │
│  │  • verifyStudentFace()                                              │  │
│  │  • deletePhoto()                                                    │  │
│  └───────┬──────────┬──────────────┬──────────────────┬────────────────┘  │
│          │          │              │                  │                    │
│          ▼          ▼              ▼                  ▼                    │
│    ┌──────────┐ ┌────────────┐ ┌──────────┐ ┌──────────────────┐          │
│    │ Local    │ │ Database   │ │ Photo    │ │ Embedding Request│          │
│    │ Storage  │ │ Helper     │ │ Upload   │ │ Service          │          │
│    │ Service  │ │ (v4.0)     │ │ Service  │ │                  │          │
│    └────┬─────┘ └──────┬─────┘ └────┬─────┘ └────────┬─────────┘          │
│         │              │             │               │                    │
│         ▼              ▼             ▼               ▼                    │
│    ┌────────────────────────────────────────────────────────────┐         │
│    │              LOCAL DEVICE RESOURCES                        │         │
│    │  ┌────────────────────────────────────────────────────┐   │         │
│    │  │ File System                                        │   │         │
│    │  │ ├── student_photos/                                │   │         │
│    │  │ │   ├── STU001_0_uuid.jpg                          │   │         │
│    │  │ │   ├── STU001_1_uuid.jpg                          │   │         │
│    │  │ │   └── STU002_0_uuid.jpg                          │   │         │
│    │  │ └── profile_photos/                                │   │         │
│    │  │     ├── USR001_profile.jpg                         │   │         │
│    │  │     └── USR002_profile.jpg                         │   │         │
│    │  └────────────────────────────────────────────────────┘   │         │
│    │  ┌────────────────────────────────────────────────────┐   │         │
│    │  │ SQLite Database (attendance_app.db)               │   │         │
│    │  │ Tables:                                           │   │         │
│    │  │ ├── users (existing)                              │   │         │
│    │  │ ├── attendance_records (existing)                 │   │         │
│    │  │ ├── student_attendance (existing)                 │   │         │
│    │  │ └── photos (NEW - v4.0)                           │   │         │
│    │  │     ├── photo_id (PK)                             │   │         │
│    │  │     ├── student_id (FK)                           │   │         │
│    │  │     ├── local_path                                │   │         │
│    │  │     ├── cloud_path                                │   │         │
│    │  │     ├── upload_id                                 │   │         │
│    │  │     ├── embedding_id                              │   │         │
│    │  │     ├── processing_status                         │   │         │
│    │  │     └── ... (9 more columns)                      │   │         │
│    │  └────────────────────────────────────────────────────┘   │         │
│    └────────────────────────────────────────────────────────────┘         │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐    │
│  │                    NETWORK / BACKEND                               │    │
│  │  ┌──────────────────────────────────────────────────────────────┐ │    │
│  │  │ Backend API Endpoints (HTTPS)                               │ │    │
│  │  │                                                              │ │    │
│  │  │ Photo Upload Service ─────→ /api/v1/photos/upload          │ │    │
│  │  │                            /api/v1/photos/batch-upload      │ │    │
│  │  │                            /api/v1/photos/upload/{id}       │ │    │
│  │  │                                                              │ │    │
│  │  │ Embedding Request ────────→ /api/v1/embeddings/generate    │ │    │
│  │  │                            /api/v1/embeddings/verify        │ │    │
│  │  │                            /api/v1/embeddings/match         │ │    │
│  │  │                            /api/v1/embeddings/student/{id}  │ │    │
│  │  │                            /api/v1/embeddings/jobs/{id}     │ │    │
│  │  └──────────────────────────────────────────────────────────────┘ │    │
│  │                                      │                             │    │
│  │                                      ▼                             │    │
│  │  ┌──────────────────────────────────────────────────────────────┐ │    │
│  │  │ Backend Services                                             │ │    │
│  │  │                                                              │ │    │
│  │  │ 1. Face Detection Service                                   │ │    │
│  │  │    └─→ Extract faces from photos (ML Kit or MTCNN)         │ │    │
│  │  │                                                              │ │    │
│  │  │ 2. Embedding Generation Service                             │ │    │
│  │  │    └─→ Generate 128-dim vectors using FaceNet model        │ │    │
│  │  │                                                              │ │    │
│  │  │ 3. Face Matching Service                                    │ │    │
│  │  │    └─→ Compare embeddings (cosine similarity)               │ │    │
│  │  │                                                              │ │    │
│  │  │ 4. Anti-Spoofing Service (optional)                        │ │    │
│  │  │    └─→ Verify liveness (photo vs live face)                │ │    │
│  │  └──────────────────────────────────────────────────────────────┘ │    │
│  │                                      │                             │    │
│  │                                      ▼                             │    │
│  │  ┌──────────────────────────────────────────────────────────────┐ │    │
│  │  │ Backend Database                                             │ │    │
│  │  │                                                              │ │    │
│  │  │ Tables:                                                      │ │    │
│  │  │ ├── photos_uploads                                           │ │    │
│  │  │ │   └─→ Uploaded photo metadata                             │ │    │
│  │  │ ├── face_embeddings                                          │ │    │
│  │  │ │   └─→ 128-dim vectors for each photo                      │ │    │
│  │  │ └── verification_logs                                        │ │    │
│  │  │     └─→ Attendance verification history                     │ │    │
│  │  └──────────────────────────────────────────────────────────────┘ │    │
│  └────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## Data Flow Sequence Diagram

```
Timeline:

1. PHOTO CAPTURE & STORAGE
   ┌─────────────────────────────────────────────────┐
   │ User captures/selects photo                     │
   │ → PhotoManagementService.saveCapturedPhoto()   │
   │ → PhotoStorageService.saveStudentPhoto()       │
   │ → DatabaseHelper.savePhoto()                   │
   │ ← Returns: Photo {id, localPath, capturedAt}   │
   └─────────────────────────────────────────────────┘
                         ▼
2. PHOTO UPLOAD
   ┌─────────────────────────────────────────────────┐
   │ PhotoManagementService.uploadPhoto()            │
   │ → PhotoUploadService.uploadStudentPhoto()       │
   │ → Backend: /api/v1/photos/upload (multipart)   │
   │ ← Backend: {uploadId, cloudPath}                │
   │ → DatabaseHelper.updatePhoto()                  │
   │ ← Returns: Photo {uploadId, cloudPath}          │
   └─────────────────────────────────────────────────┘
                         ▼
3. EMBEDDING PROCESSING REQUEST
   ┌─────────────────────────────────────────────────┐
   │ PhotoManagementService.requestEmbeddingProcessing()
   │ → EmbeddingRequestService.requestEmbeddingGeneration()
   │ → Backend: /api/v1/embeddings/generate (async) │
   │ ← Backend: {jobId, status: "processing"}       │
   │ → DatabaseHelper.updatePhoto()                  │
   │ ← Returns: jobId for polling                    │
   └─────────────────────────────────────────────────┘
                         ▼
4. BACKEND PROCESSING (Async)
   ┌─────────────────────────────────────────────────┐
   │ Backend async process:                          │
   │ 1. Extract face from photo                      │
   │ 2. Generate 128-dim FaceNet embedding           │
   │ 3. Store in embeddings database                 │
   │ 4. Calculate photo quality metrics              │
   │ Status: pending → processing → completed        │
   └─────────────────────────────────────────────────┘
                         ▼
5. POLLING JOB STATUS
   ┌─────────────────────────────────────────────────┐
   │ App periodically polls status                   │
   │ → /api/v1/embeddings/jobs/{jobId}              │
   │ ← Backend: {status, progress, ...}              │
   │ Repeat until status = "completed" or timeout    │
   │ → DatabaseHelper.updatePhoto()                  │
   │   (embeddingId, photoQuality, processingStatus) │
   └─────────────────────────────────────────────────┘
                         ▼
6. FACE VERIFICATION (During Attendance)
   ┌─────────────────────────────────────────────────┐
   │ Teacher captures student photo                  │
   │ → PhotoManagementService.verifyStudentFace(    │
   │    studentId, photoPath, authToken)             │
   │ → EmbeddingRequestService.verifyFace()          │
   │ → Backend: /api/v1/embeddings/verify (multipart)
   │                                                  │
   │ Backend Processing:                             │
   │ 1. Extract face from captured photo             │
   │ 2. Generate embedding for captured face         │
   │ 3. Check student's enrolled embeddings          │
   │ 4. Calculate similarity scores                  │
   │ 5. Compare with threshold                       │
   │                                                  │
   │ ← Backend: {matchScore, verificationStatus}     │
   │                                                  │
   │ App Decision:                                   │
   │ if (matchScore >= 0.6) → Mark PRESENT           │
   │ else                    → Prompt retry            │
   └─────────────────────────────────────────────────┘
```

## Service Class Diagram

```
┌──────────────────────────────────────────────────────────┐
│                PhotoStorageService                       │
├──────────────────────────────────────────────────────────┤
│ - _photosDirectory: String                               │
│ - _profilePhotosDirectory: String                        │
├──────────────────────────────────────────────────────────┤
│ + saveStudentPhoto(path, id) → String?                  │
│ + saveProfilePhoto(path, userId) → String?              │
│ + getStudentPhotoPaths(studentId) → List<String>        │
│ + getProfilePhotoPath(userId) → String?                 │
│ + deletePhoto(path) → bool                              │
│ + photoExists(path) → bool                              │
│ + getStorageUsage() → int                               │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│               PhotoUploadService                         │
├──────────────────────────────────────────────────────────┤
│ - _baseUrl: String                                       │
├──────────────────────────────────────────────────────────┤
│ + uploadStudentPhoto(...) → Map? {upload_id, ...}        │
│ + batchUploadPhotos(...) → List<Map?>                    │
│ + getUploadStatus(id, token) → Map?                      │
│ + deleteUploadedPhoto(id, token) → bool                  │
│ + requestEmbeddingProcessing(...) → Map? {job_id}        │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│            EmbeddingRequestService                       │
├──────────────────────────────────────────────────────────┤
│ - _baseUrl: String                                       │
├──────────────────────────────────────────────────────────┤
│ + requestEmbeddingGeneration(...) → Map? {job_id}        │
│ + verifyFace(...) → FaceVerificationResult?              │
│ + matchFace(...) → List<Map>                             │
│ + getStudentEmbeddings(...) → List<EmbeddingResponse>    │
│ + getJobStatus(id, token) → Map?                         │
│ + reenrollStudent(...) → bool                            │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│           PhotoManagementService                         │
├──────────────────────────────────────────────────────────┤
│ - _dbHelper: DatabaseHelper                              │
│ - _photoStorage: PhotoStorageService                     │
│ - _photoUploader: PhotoUploadService                     │
│ - _embeddingService: EmbeddingRequestService             │
├──────────────────────────────────────────────────────────┤
│ + saveCapturedPhoto(...) → Photo?                        │
│ + uploadPhoto(id, token) → Photo?                        │
│ + verifyStudentFace(...) → FaceVerificationResult?       │
│ + requestEmbeddingProcessing(...) → String? (jobId)      │
│ + deletePhoto(...) → bool                                │
│ + batchUploadPhotos(...) → List<Photo>                   │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│              DatabaseHelper (Extended)                   │
├──────────────────────────────────────────────────────────┤
│ + savePhoto(...) → bool                                  │
│ + getPhotoById(id) → Map?                                │
│ + getStudentPhotos(studentId) → List<Map>               │
│ + getPendingUploadPhotos() → List<Map>                  │
│ + getPendingEmbeddingPhotos() → List<Map>               │
│ + updatePhoto(...) → bool                               │
│ + deletePhoto(id) → bool                                │
│ + deleteAllStudentPhotos(studentId) → bool              │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│                 Model Classes                            │
├──────────────────────────────────────────────────────────┤
│ Photo                                                    │
│ ├─ id: String                                            │
│ ├─ studentId: String                                     │
│ ├─ localPath: String                                     │
│ ├─ uploadId: String?                                     │
│ ├─ processingStatus: String                              │
│ └─ ... (10 more fields)                                  │
│                                                          │
│ FaceVerificationResult                                   │
│ ├─ studentId: String                                     │
│ ├─ matchScore: double (0.0-1.0)                          │
│ ├─ verificationStatus: String                            │
│ └─ isVerified: bool (getter)                             │
│                                                          │
│ EmbeddingResponse                                        │
│ ├─ embeddingId: String                                   │
│ ├─ embeddingVector: List<double>? (128 values)           │
│ ├─ photoQuality: String                                  │
│ └─ ... (5 more fields)                                   │
└──────────────────────────────────────────────────────────┘
```

## Component Dependency Graph

```
                    Screens
     (PhotoPickerScreen, TakeAttendanceScreen, etc.)
                      │
                      ▼
           PhotoManagementService
              (Orchestrator)
                      │
        ┌─────┬───────┼────────┬───────┐
        │     │       │        │       │
        ▼     ▼       ▼        ▼       ▼
   Photo  Database  Photo   Embedding  Other
  Storage  Helper  Upload  Request   Services
  Service Service Service  Service
        │     │       │        │
        └─────┼─────────────────┘
              │
        ┌─────┴──────┐
        ▼            ▼
  File System   SQLite DB
  (Local)       (Local)
                     │
                     ▼
              Backend API (HTTPS)
                     │
        ┌────────────┼────────────┐
        ▼            ▼            ▼
    File Store  Embeddings    Photo DB
    (Cloud)     (Backend)     (Backend)
```

This architecture provides:
- ✅ Clean separation of concerns
- ✅ Single responsibility principle
- ✅ Easy to test each component
- ✅ Easy to replace/update services
- ✅ Scalable design
- ✅ Flexible for future enhancements
